# Implementation Plans

Generated by the improve skill on 2026-09-09, against commit `30e3782`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

**Context for whoever picks these up.** The package is unpublished
(`version: 0.0.0`, 7 commits, no npm release). Several of these changes touch
`@public` types and are materially cheaper now than after the first release —
001, 008 and the direction items in particular. There is also a theme worth
naming: four things this codebase *asserted about itself* in comments and
README prose turned out to be false (beta-API containment, per-section error
isolation, a working retry button, full localization). Plans 003, 006 and 008
each fix code **and** the sentence that misdescribed it. When you change
behaviour here, fix the comment in the same commit.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make "Mark as done" stick for items with a future timestamp | P1 | S | — | DONE |
| 002 | Stop a slow dismissals load from discarding a tick | P1 | S | — | DONE |
| 003 | Make one failing source cost one card, not the whole pane | P1 | S | — | DONE |
| 004 | Pin the release tooling so the publish job runs only locked code | P1 | S | — | DONE |
| 005 | Typecheck in CI, and typecheck the test files too | P1 | S | — | DONE |
| 006 | Fix the public docs and identifiers the rename left behind | P2 | S | — | DONE |
| 007 | Put the jsdom harness to work on the "mark as done" path | P1 | M | 001, 002, 003 | DONE |
| 008 | Actually contain the beta Sanity APIs the sources depend on | P1 | M | 003, 007 | DONE |
| 009 | Make async structure resolvers get the list item and the redirect | P2 | M | 006, 007 | DONE |
| 010 | Export a `buildDigest` helper for the README's digest recipe | P1 | S | — | DONE (merged) |
| 011 | Investigate a live badge on the Structure tool's own nav icon | P2 | M | — | DONE (merged) |
| 012 | Investigate and prototype a team-wide "who's sitting on what" view | P2 | L | — | DONE (merged) |
| 013 | Bulk "save a copy to my todos" selection action | P2 | S | — | DONE (merged) |
| 014 | Stop a finished todo from silently reappearing after 90 days | P1 | M | — | DONE (merged) |
| 015 | A pure helper (+ README recipe) for finding departed editors' leftover state | P2 | S | — | DONE (merged) |
| 016 | Let the open count escape the pane (`useInboxOpenCount`) | P2 | L | — | DONE (merged) |
| 017 | A live badge on the Studio navbar, for real | P2 | S | 016 | REJECTED (built, verified working, then pulled by the maintainer — see plan file) |
| 018 | Ship the team-wide "who's sitting on what" view as a real Studio tool | P2 | L | — | DONE (merged) |
| 019 | "Cleared" means Sanity said so — split resolution from acknowledgement | P1 | XL | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

Plans 014–015 came from a lifecycle-focused `/improve` run (2026-09-13,
against commit `32e028c`) — the maintainer asked specifically how the plugin
holds up "over a long time horizon... things get finished, some stay
unfinished." 014 is a real correctness bug (not a direction option) found
during that pass; 015 is a direction/tooling gap in the same spirit as
`buildDigest`. Neither depends on the other or on 010–013.

Plan 019 (2026-09-14, against commit `fe426b3`) reworks Open/Done/Snoozed into
Open/Cleared/Snoozed, splitting a real, source-confirmed resolution from a
personal "Acknowledge" that changes nothing in Sanity. Its own Step 3 design
decision — whether `limit` should cap `openTasks`' open and recently-closed
tasks together or separately — was first shipped as "together," then proven
wrong during this plan's own live browser verification: closing a task in a
Studio that already had `limit`-or-more open tasks silently sliced the
just-closed task off the end of the result before it ever reached Cleared.
Fixed to two independently-capped GROQ slices; see the code comment above
`QUERY` in `openTasks.ts`.

Plans 016–018 (2026-09-14, against commit `d2cc473`) turn two earlier spikes
into production plans, at the maintainer's request: 016+017 are Plan 011's
nav-badge spike, split into a prerequisite refactor (016, extracting the
open count into a shared, always-mounted provider — **017 depends on 016**
and must not be executed first) and the actual badge feature (017); 018 is
Plan 012's team-view spike, shipped as a new, separate, opt-in Studio tool.
Batch-1's Direction A is now half-implemented by 016 (see that direction's
own updated note below); Direction B was re-investigated and downgraded —
its premise no longer holds against the current 4-source lineup, see its
own note below — no plan was written for it.

Plans 010–013 came from a `next`/roadmap `/improve` run (2026-09-13, against
commit `2218e06`), scoped to the direction category only — see "Direction
options, 2026-09-13 run" below for the findings behind them. All four were
approved by the maintainer in one batch ("All 4 sounds good"); none depend on
each other or on 001–009.

## Reviewed by `/improve execute`, awaiting merge

Dispatched executor subagents, isolated `git worktree` each. Reviewed like a
PR against the plan's own done criteria — re-ran every verification command
in the worktree myself, checked scope compliance, read the full diff, audited
the new tests. **Not merged** — merging is the maintainer's call, not the
advisor's; each branch sits ready in its worktree below.

010–013 (reviewed here originally) have since been merged — see "Merged to
`main` (batch 2)" below. Their review notes are kept for the record.

| Plan | Verdict | Branch | Worktree |
|---|---|---|---|
| 014 | APPROVE | `advisor/014-fix-todo-dismissal-ttl` | `.claude/worktrees/agent-ae0eb3f36c813cd41` |
| 015 | APPROVE | `advisor/015-stale-editor-doc-cleanup` | `.claude/worktrees/agent-af96b6523ce15f73d` |
| 010 | APPROVE (merged) | `advisor/010-export-digest-builder` | `.claude/worktrees/agent-a8ad296d336fba758` |
| 011 | APPROVE (merged) | `advisor/011-investigate-nav-badge` | `.claude/worktrees/agent-afd47c0f454a4c3cc` |
| 013 | APPROVE (merged) | `advisor/013-bulk-save-to-todos` (retry, on top of `decd612`) | `.claude/worktrees/agent-a43bd630a850bdca6` |
| 012 | APPROVE (merged) | `advisor/012-team-wide-view` (retry, on top of `decd612`) | `.claude/worktrees/agent-abb131c3214e5ad07` |

**014 review notes**: Clean, in-scope diff vs. its true merge-base
(`acbb3ec`) — exactly the 7 planned files (`dismissals.ts`,
`useDismissals.ts`, `types.ts`, `sources/todos.ts`, `Inbox.tsx`,
`dismissals.test.ts`, `README.md`), confirmed with `git diff --stat
$(git merge-base HEAD main)`. Re-ran typecheck/lint/`dismissals`
tests (28/28)/build myself in the worktree — all green. Read the full diff:
matches the plan exactly, no surprises. Read all 3 new tests: real
assertions (exempt source survives the TTL, a non-exempt source in the same
state still gets pruned, omitting the new parameter preserves old behavior
exactly) — none vacuous. The executor's worktree was, again, provisioned
from a stale base (`acbb3ec`, ~14 commits behind); it correctly judged this
non-blocking since none of the intervening commits touch
`pruneDismissals`/`useDismissals`/the `InboxSource` interface's core shape,
and verified that judgment explicitly rather than assuming it — good
practice, flagged for the record rather than as a defect.

**015 review notes**: Clean, in-scope diff vs. its correct merge-base
(`32e028c`, matches current `main` exactly) — exactly the 4 planned files
(`src/staleEditorDocs.ts`, `src/staleEditorDocs.test.ts`, `src/index.ts`,
`README.md`). Re-ran typecheck/lint/`staleEditorDocs` tests (5/5)/full suite
(180/180)/build myself — all green. Confirmed `findStaleEditorDocuments`
imports nothing from `sanity`/`@sanity/client` — matches `buildDigest`'s
zero-dependency contract. All 5 tests are real, including one that actually
proves the correctness claim in the function's own doc comment (a user id
containing a literal `.` round-trips via `slice`, unlike a naive
split-on-first-`.` approach) rather than just asserting the happy path. This
executor caught its own worktree's stale-base problem *before* the drift
check flagged it as a false mismatch, diagnosed the real cause (branch
forked from `acbb3ec`, not `32e028c`), and recreated its own branch from the
correct commit — the cleanest handling of this recurring class of issue
across every plan executed this session.

**010 review notes**: Clean, in-scope diff (`README.md`, `src/digest.ts`,
`src/digest.test.ts`, `src/index.ts` only — `git status` in the worktree
confirms nothing else touched). `buildDigest` imports only from
`./store/dismissals`, `./store/snoozes`, `./inbox/types`, `./inbox/splitItems`
— no `sanity`/`@sanity/client`, matching the plan's zero-dependency
requirement. Re-ran `typecheck`/`lint`/full test suite (151 tests, incl. the
5 new `digest.test.ts` cases — read them; each asserts real dismiss/snooze/
combine/empty-state behavior, not a vacuous pass)/`build` myself in the
worktree — all green. The executor's own worktree was provisioned one commit
behind `main` (base `acbb3ec`, missing `2218e06`); it correctly verified this
didn't affect any file it touched before proceeding — a good catch, not a
defect.

**011 review notes**: In-scope diff only (`test-studio/sanity.config.ts`,
three new `test-studio/plugins/*.tsx` spike files, this plan file) — re-ran
`npm run typecheck` myself in the worktree, exit 0. Prototyped and compared
*both* candidate mechanisms, not just one: `Tool.icon` (real API, but
confirmed — by actually running the Studio and inspecting network traffic,
not just reading types — that Sanity's own `ToolMenu` never renders it as an
icon under a normal desktop/few-tools layout, so it silently never mounts)
vs. `studio.components.navbar` (real, composes via `renderDefault`, verified
live-updating with a real document create through the MCP Sanity tools).
Clear recommendation: proceed with `navbar`, drop `Tool.icon`, with a
concrete 4-item scope list for the eventual production plan. High-quality
spike — findings are evidence-based throughout, including the
network-request-level confirmation, not just code reading.

**Flag, not a scope violation**: the executor created a new, throwaway
Sanity project (`lab81vuo`) via the Sanity MCP tools to actually run and
visually verify the spike Studio, since that's the only way to genuinely
confirm a badge renders — this wasn't in the plan's file-scope list (which
only covers repo files) but is a reasonable call for a spike whose whole
point is empirical verification, and it was disclosed prominently rather
than done silently. It holds two throwaway test documents and nothing else.
**Your call**: leave it for now, or have me delete it — I have not deleted it.

**013 review notes (retry, against `decd612`)**: In-scope diff only
(`MergedList.tsx`, `SelectionActions.tsx`, `en-US.ts`, `MergedList.test.tsx`
— confirmed via `git status`/`git diff --stat`). Re-ran
`typecheck`/`lint`/full suite (170/170, incl. the 4 new cases)/`build`
myself — all green. Read the diff and all 4 new tests: real assertions
(exactly-one-creator gating, per-row title/description/dueBy passed through
correctly across three different sources, undo toast + selection clear) —
none vacuous. Two documented, in-bounds deviations, both correct: (1) placed
`soleCreator`/`confirmSaveToTodos` after `creators` rather than "near
confirmAssign" as the plan's prose said, since the plan's own code needs
`soleCreator` declared first — the plan's literal placement instruction
would have been a temporal-dead-zone bug; (2) wrapped the `create` call in
`Promise.resolve().then(...)` for a synchronously-throwing source, exactly
per the plan's own pre-authorized STOP-condition guidance. Neither touches
any existing capability.

**012 review notes (retry, against `decd612`)**: In-scope diff only
(`plans/012-team-wide-view.md`, `test-studio/plugins/teamViewSpike.tsx`,
`test-studio/sanity.config.ts`) — re-ran `typecheck` myself, exit 0. Read the
prototype: a plain Studio tool that calls `unpublishedDrafts()`/
`openTasks({onlyMine: false})` directly and groups by
`item.assignee?.label ?? 'Unassigned'` — clean, no shortcuts, matches the
plan's own scope exactly (no assign/resolve/snooze wired up). The real value
here is Step 3's finding: it overturns the plan's own framing — cross-editor
dismissal state turns out to be **actively wrong** to fold in for
`unpublishedDrafts`/`upcomingReleases` (neither has `resolve`; a personal
tick there was never global completion) and unnecessary for `openTasks`
(completion already lives on the task's own `status`). That's a materially
better answer than either option the plan posed, reasoned from the actual
module comments this plugin already carries. Recommendation (proceed to a
production plan, with a 4-item scope list) is well-grounded.

**Branch note**: this retry's commit could not land on `advisor/012-team-wide-view`
directly — that branch was still checked out in the *first* (blocked,
zero-real-changes) attempt's sibling worktree, and git refuses two
worktrees on one branch. The executor correctly landed it on its own
worktree branch instead of forcing anything. I've since removed the stale
first-attempt worktree (nothing but the plan file, already fully subsumed
into this retry's own copy) and renamed this retry's branch to
`advisor/012-team-wide-view`, so the name now points at the real work.

## Root cause behind 012/013's first (resolved) blocks

Both plans are now **DONE/reviewed** — see the table and review notes above.
This section is the historical record of *why* their first attempts blocked,
kept so the underlying lesson (uncommitted work + isolated worktrees) isn't
re-learned the hard way next time.

Two execution attempts on plan 013, both stopped correctly (no bad edits made
either time):

1. **Attempt 1**: worktree provisioned at `acbb3ec`, one commit behind `main`
   (`2218e06`) — the missing commit touches exactly this plan's in-scope
   files. Executor caught it, stopped, made no edits.
2. **Attempt 2** (after instructing a rebase onto `main` first): rebase
   succeeded, worktree HEAD landed exactly on `main`'s tip — but the plan's
   own "Current state" excerpts still didn't match. `InboxItem` at `main`'s
   tip has **no** `description`, `dueBy`, or `assignee` field; `InboxSourceResult`
   has **no** `update` field and `assign` has no `unassign`. The plan's Step 1
   code (`row.item.description`, `row.item.dueBy`) references fields that
   don't exist in *any commit* — they'd fail to typecheck.

**Root cause, confirmed**: this repo's main working tree (outside any
worktree) has carried 18 modified-but-uncommitted files all session —
`src/inbox/types.ts`, `MergedList.tsx`, `InboxRow.tsx`, `SelectionActions.tsx`
and others — containing real, working feature additions (per-row assignee
avatars, a todo edit dialog with description/due date, unassign) that were
never committed. Plans 010–013 were authored by reading *that* working tree
(the accurate, live state of the pane), not `main`'s committed history. An
isolated `git worktree` — which `execute` requires for safety — only ever
sees committed commits, so any plan whose "Current state" quotes one of
those 18 uncommitted files can mismatch an executor's worktree even with zero
drift since the plan was written.

**Plan 013 was BLOCKED** on this (at the time) — its core premise (saving a
row's `description`/`dueBy` into a todo) depended on `InboxItem` fields that
didn't exist in any commit yet. Resolved by committing the pending work as
`decd612` (see below) and re-running the plan against it — now DONE, see the
review notes above.

**012 confirmed the same root cause, independently, and was also BLOCKED at
the time.** Its executor stopped before Step 2 (the prototype) because
`unpublishedDrafts.ts`'s `assignee`/`openTaskByTarget`/`assigneesById` join —
the "already computed, just re-sort it" building block the whole plan's
Step 2 rests on — did not exist in any commit reachable from `main`
(`git log --all -S"assigneesById"` found zero commits). It did exist,
verified directly, in the main checkout's own uncommitted
`unpublishedDrafts.ts` — so the plan was accurate against the live pane,
wrong against `main` at the time. Step 1 of 012 (confirming cross-editor
dismissal reads via a real MCP query — genuinely useful, independent of this)
completed and is written into `012-team-wide-view.md`'s own "## Findings"
section. Also resolved by `decd612` — retried, now APPROVE, see notes above.

## Merged to `main` (batch 3: 014, 015)

Reviewed, approved, and merged at the maintainer's instruction (2026-09-13).
`main` is at `68d29d0`; typecheck, lint, 183 tests and build all green after
the final merge. Nothing has been pushed. Both merges were clean auto-merges
— no conflicts.

| Plan | Merge commit | Branch |
|---|---|---|
| 014 | `e7a8e6f` | `advisor/014-fix-todo-dismissal-ttl` |
| 015 | `68d29d0` | `advisor/015-stale-editor-doc-cleanup` |

## Merged to `main` (batch 2: 010, 013, 011, 012)

Reviewed, approved, and merged at the maintainer's instruction (2026-09-13).
`main` is at `32e028c`; typecheck, lint, 175 tests and build all green after
the final merge. Nothing has been pushed. One real merge conflict, resolved
by hand: 011 and 012 each independently added their own new `test-studio`
workspace to `sanity.config.ts` from the same base commit — kept both
workspaces side by side (`badgeSpike` and `teamViewSpike`), no logic
conflict, just two independent list entries landing in the same spot.

| Plan | Merge commit | Branch |
|---|---|---|
| 010 | `fafe09d` | `advisor/010-export-digest-builder` |
| 013 | `6310b5d` | `advisor/013-bulk-save-to-todos` |
| 011 | `c691a14` | `advisor/011-investigate-nav-badge` |
| 012 | `32e028c` | `advisor/012-team-wide-view` |

## Merged to `main` (batch 1)

Five plans reviewed, approved and merged at the maintainer's instruction
(2026-09-09). `main` is at `99384a2`; typecheck, lint, 40 tests and build all
green after the final merge. Nothing has been pushed.

| Plan | Merge commit | Branch |
|---|---|---|
| 009 | `c9b9709` | `advisor/009-async-structure-resolvers` |
| 001 | `58ceb91` | `advisor/001-future-timestamp-dismissals` |
| 002 | `0dab800` | `advisor/002-dismissals-load-race` |
| 004 | `dc4d6fa` | `advisor/004-pin-release-tooling` |
| 005 | `3480af1` | `advisor/005-typecheck-in-ci` |
| 006 | `99384a2` | `advisor/006-fix-stale-public-docs` |
| 003 | `8a13d7f` | (merged directly to main) |
| 007 | `35ca723` | `advisor/007-component-tests-inbox` |

Review notes carried forward:

- **001** — the executor found a defect in *this plan*: Test-plan item 1 asked for an
  assertion that `isDismissed(..., <future>)` returns `true` while Step 3 mandated no
  behaviour change to that function, which makes the assertion impossible. It wrote a
  test proving the real fix instead and flagged it. The plan was wrong; the code is
  right. **Amended** in `001-fix-future-timestamp-dismissals.md` so a re-run does not
  hit the same contradiction.
- **001, open gap** — the new test is a pure-function test. Reverting `InboxSection`
  to `item.timestamp` leaves the suite green; only the grep criterion and the type
  catch that. Plan 007 must assert the open/done split reads `changedAt`. Added there.
- **004** — the dry run exits 1, not 0, with `ERELEASEBRANCHES`. Verified independently:
  the GitHub repo is empty (`isEmpty: true`, no default branch), so `main` does not exist
  on the remote. Environmental, not a defect — the criterion that mattered (preset
  resolution) passes with 26 plugins loaded and zero missing modules. Re-verify on the
  first real CI run, once anything has been pushed.
- **004, lockfile** — 13,401 changed lines looked alarming; verified independently that
  zero pre-existing packages changed version or were removed, 406 added, all carrying
  integrity hashes.
- **002** — the executor found a second gap in *this plan*: `dirtyRef` is cleared
  synchronously by the persist effect before its commit resolves, so it cannot answer
  "has local state ever diverged from a fresh mount". It added `hasLocalEditRef` for
  that, correctly. It also caught its own ordering bug (setting `loadedRef` before a
  `JSON.parse` that can throw). The merged result is stronger than the plan specified:
  the `loadedRef` guard *defers* a premature write rather than dropping it.
- **008** — the executor hit this plan's own STOP condition (oxlint flags a syntactically
  conditional hook call even when the condition is module-stable) and, instead of stopping,
  found a restructuring that satisfies the intent: `optionalHook(name, fallback)` returns
  either the real export or a same-shape fallback hook, so the call site invokes it
  unconditionally every render and distinguishes the two by reference identity. No lint
  suppression. Better than what the plan sketched; approved on merit.
- **007/008, process note** — both interrupted first attempts leaked uncommitted files into
  the *main* working tree (worktrees share the git dir). Twice this shadowed the reviewed
  version after a merge — once deleting merged test files, once overriding `capability.ts`
  with a stale signature that broke typecheck. Both caught by re-verifying after the merge
  and recovered with `git checkout --`. Lesson: always re-run the full gate on `main` after
  merging, not only inside the worktree.
- **009** — landed, but its **manual Studio verification (step 5) was never performed**:
  the executor was interrupted by a rate limit before reporting, so nobody has watched
  a delayed resolver actually redirect in a browser. The unit tests cover the two
  mechanisms (list item on the async branch; availability flipping after the promise
  settles), and the `asyncDelayed` workspace is in `test-studio/sanity.config.ts`
  ready to run. Open `/async-delayed/structure` and confirm the URL becomes
  `/structure/structure-inbox` after ~750ms, and that a deep link is not hijacked.
- **005** — the deliberate-error probe answered the open question: `npm run build`
  **passes** with a type error in a test file, confirming the dist build was never a
  partial safety net for tests. The new CI step is the only thing that catches them.
- **007** — branched as `advisor/007-component-tests-inbox`, not the plan's stated
  `advisor/007-component-tests`: that branch name was already checked out by another
  worktree (`agent-afac11799ec5bd685`), clean and stale (pointed at `8a13d7f`, before
  009 merged) — not force-moved, to avoid clobbering whatever owns it. `@testing-library/react`'s
  auto-cleanup never registers with `globals: false` in `vitest.config.ts`; every new
  test file needs an explicit `afterEach(cleanup)` or DOM (and, for renderHook, pending
  effects) leak into the next test — the `useDismissals` "persists a dismissal" test
  only failed once cross-test leakage was fixed. `@testing-library/user-event` is not a
  dependency, so selection is driven with `fireEvent` on the checkbox `<input>`, found
  via its `aria-labelledby` back-reference to the row's title text (the checkbox has no
  wrapping `<label>`). `useTranslation` returns raw i18n keys with no instance in
  context, confirmed harmless; `RelativeTime` (`useRelativeTime` from `sanity`) needs a
  full Studio locale context that free-floating `ThemeProvider`+`RouterProvider` does
  not provide, so it is mocked at the module level for the one test that needs a real,
  divergent `item.timestamp`. `useClient`/`useCurrentUser` are mocked via `vi.mock`;
  `useClient`'s mock is built with `vi.hoisted` rather than `vi.mocked(sanity.useClient)`
  to dodge the `no-deprecated` lint rule on `useClient`'s no-arg overload. 52 tests total
  (baseline 42 + 10 new, plan asked for ≥49).

## Dependency notes

- **001–006 are independent of each other** and can be done in any order, or in
  parallel by different executors. They are grouped first because each is small,
  high-confidence, and self-verifying.
- **004 and 005 both edit `.github/workflows/main.yml`** but different jobs
  (004 the `release` job, 005 the `build` job). Doing them in the same worktree
  sequentially is fine; doing them in parallel will conflict.
- **007 depends on 001, 002 and 003** because its tests assert the behaviour
  those plans produce. If 007 runs first, its tests must be written as
  characterization tests of today's behaviour and then updated — more work, so
  prefer the stated order.
- **008 depends on 003**: containment routes a missing capability into an error
  card, which is only survivable once the error boundary actually isolates a
  section. Landing 008 first leaves the pane crashable.
- **008 and 009 both depend on 007** — they are the two restructurings, and
  they are the reason to build the test harness at all.
- **009 depends on 006** only for the `homeAvailable` → `inboxAvailable` rename;
  if 006 is skipped, use whichever field name is live.

## Verification baseline

Every plan uses these. All pass at `30e3782`:

| Purpose | Command |
|---|---|
| Typecheck | `npx tsc --noEmit -p tsconfig.json` |
| Lint | `npm run lint` |
| Tests | `npx vitest run` (34 tests, 3 files) |
| Build | `npm run build` |

Note that `npm run typecheck` does not exist until plan 005 adds it.

## Findings considered and rejected

- **`npm audit`'s 16 advisories (15 moderate, 1 high)**: every path terminates
  in dev-only tooling (`@sanity/cli` via the test studio, `vitest`). The
  published tarball is `dist/` only, with two runtime deps that appear in no
  advisory. `npm audit --omit=dev` is clean, and `npm audit fix --force` would
  downgrade `sanity` to v5 and break the v6 peer target. Wait for upstream.
- **Lossy user-id → document-id mapping** (`useDismissals.ts:38-40`): two users
  collide only if their Sanity ids differ *solely* in characters outside
  `[A-Za-z0-9._-]`, which current Sanity id formats never produce. The fix
  orphans every existing dismissals document. Revisit only if Sanity's id
  format changes — and if so, do it before the package has real adoption.
- **Percent-encoding document ids in the history URL** (`authoredBy.ts:28`):
  Sanity's `_id` alphabet already excludes every path-breaking character, so
  there is no live vulnerability. Worth one line of hardening bundled into
  another change to that file, not a plan of its own.
- **Tests for `InboxRow`, `SelectionActions`, `RelativeTime`, `SectionCard`,
  and `upcomingReleases`**: presentational or thin adapters; tests would mostly
  assert the mock. They are covered incidentally by plan 007's `InboxSection`
  tests.
- **Extracting a shared widget contract with `sanity-plugin-cockpit`**: cockpit
  ships zero widgets, sits on `@sanity/ui` v3 against this repo's v4, and
  `InboxSource` has diverged past cosmetics (`name` namespaces dismissals;
  `resolve` encodes whether "done" is global). The only real overlap is a GROQ
  query and `authoredBy.ts` — copy-paste, not a shared abstraction. Declining
  is the recommendation; revisit only when the same widget is genuinely wanted
  on both surfaces.
- **Keyboard shortcuts for the selection bar / row actions**: explicitly
  rejected by the maintainer — this plugin should not add shortcuts that risk
  colliding with Sanity Studio's own. Do not re-propose; revisit only if
  Studio ships a documented, reservable keybinding namespace for plugins.
- **Sanity Workflows as an inbox source**: raised and deferred (not this
  session) — there is no stable Workflows schema to build a source against
  yet. Revisit once Workflows' document shape is stable enough to query.

## Known findings with no plan yet

Real, vetted, and deliberately not planned in this batch — recorded so they are
not re-audited from scratch:

- **`onlyMine` filters after the query limit** (`unpublishedDrafts.ts:102-116`):
  authorship filtering runs on the newest `limit` drafts dataset-wide, so "your
  drafts" is routinely empty on a busy dataset — indistinguishable from
  all-clear. Fix is over-fetch-then-slice, or page until `limit` matches.
  Effort S/M, MED risk (more requests per refresh).
- **"Try again" is inert** (`SectionCard.tsx:38-40`): sources report errors
  through `result.error`, but the handler clears `caughtError`, which is already
  null. Either hide the button for reported errors (S), or add
  `refresh?: () => void` to `InboxSourceResult` and wire it through (M) — the
  latter is also direction item A below, and is cheaper before 1.0.
- **Source titles cannot be localized** while the README says every string can
  (`openTasks.ts:65`, `unpublishedDrafts.ts:67`, `upcomingReleases.ts:27`, plus
  `'Overdue'`/`'Due'`). Needs `InboxSource['title']` to accept `{key, ns}`.
- **Duplicate source `name` collides** in counts, React keys and the dismissal
  namespace (`Inbox.tsx:46`), so `sources: [unpublishedDrafts({types: ['post']}),
  unpublishedDrafts({types: ['page']})]` misbehaves. Accept a `name` override.
- **Availability keyed by tool name only** (`inboxAvailability.ts:15`) —
  multi-workspace cross-talk. Noted in plan 009's Maintenance notes.
- **Dismissals lost if the pane unmounts mid-resolve**
  (`InboxSection.tsx:104-113` + `useDismissals.ts:126`).
- **`openTasks` ordering** (`openTasks.ts:40`) sorts undated stale tasks ahead
  of imminent ones, so the 10-row window fills with dormant work.
- **Privacy note for consumers, not a code fix**: the dismissals document lands
  in the *customer's* dataset as an unregistered type, so per-editor activity —
  what each person ticked, with timestamps — is readable by anyone with dataset
  read access and appears in exports. The storage choice is sound (the
  `/users/me/keyvalue` endpoint rejects plugin keys); consumers should just be
  told in the README.
- **No `AGENTS.md`/`CLAUDE.md`**, and several release- and build-breaking
  invariants live only in prose comments: conventional commits are load-bearing
  for semantic-release; `test-studio` consumes `dist/`, not `src/`;
  `structureInbox()` must come after `structureTool()`; the dismissals type must
  stay unregistered. Worth one file.

## Direction options (maintainer's call, not queued work)

- **A. Make sources refreshable, and let the count escape the pane.**
  `InboxSourceResult` has `resolve` (write) but no re-read, so the pane editors
  leave open all day is frozen at mount. `Inbox.tsx` already hand-rolls an
  `onCount` callback to lift the total out of each section — exporting
  `useInboxCount(sources)` would let a navbar or dashboard widget show "3
  waiting" without rendering the pane. Additive and cheap now; breaking after
  1.0.
  **Update, 2026-09-14: the "let the count escape the pane" half is now
  planned** — Plan 016 (`plans/016-inbox-open-count-provider.md`) ships
  exactly this as `useInboxOpenCount()`, and Plan 017 builds a live navbar
  badge on top of it. The "make sources refreshable" half (a stale pane's
  own `Try again`/manual re-fetch) is **not** covered by either plan — it
  remains open, and overlaps with the existing "'Try again' is inert"
  finding below; a future plan could tackle both together.
- **B. Spike a data-first source descriptor before publishing.** `useItems` as
  a hook is why no source has a test — you need a renderer. Two of three
  sources are already just query + mapper. A `{query, params, toItem}` shape
  alongside the hook makes the common case pure and testable, with the hook as
  the escape hatch. Cost: two ways to declare a source. Scope as a spike.
  **Re-investigated, 2026-09-14 — premise no longer holds, downgraded to
  rejected.** This was true of the 3-source lineup on 2026-09-09. Checked
  against the current 4 built-in sources: `openTasks.ts` (241 lines) and
  `unpublishedDrafts.ts` (349 lines) are now both genuinely complex —
  multi-query joins, `assign`/`resolve`/`assess` mutations, addon-dataset
  reads — not "query + mapper." `todos.ts` (91 lines) has no GROQ query at
  all (a client-side store, not a fetch), so a `{query, params, toItem}`
  shape doesn't describe it either. Only `upcomingReleases.ts` (99 lines)
  still fits the simple shape today — one source out of four, not "two of
  three." The stated payoff ("makes *the common case* pure and testable")
  no longer matches which case is actually common in this codebase now.
  **Not recommending a spike.** Revisit only if a future built-in source is
  genuinely simple query+mapper shaped *and* there are enough of those to
  matter — a one-off shape for a single source isn't worth a second way to
  declare a source.
- **C. Snooze.** The store already compares timestamps and prunes by TTL, so
  "not now" is close to a predicate change. But it adds a second verb, which
  reopens the deliberate one-verb decision recorded in
  `SelectionActions.tsx:18-28`. Technical cost low, design cost real.
- **D. Bounding the Done view by recency.** Raised in review: Done accumulates,
  mainly for `unpublishedDrafts` (a closed task leaves its source query on
  reload; a dismissed draft does not). Note that a "remove from Done" action
  cannot shrink stored state — clearing the record returns the item to Open, so
  a third action would mean a new permanent flag. Showing only recently-done
  items is the cheaper answer and preserves the one-verb rule. Not obviously
  worth doing until it annoys someone in real use.

## Direction options, 2026-09-13 run

Scoped `next`/roadmap audit against commit `2218e06`, prompted by "make the
plugin more intuitive — what adds value for an individual or a team so they
feel on top of their content." Four grounded findings, all approved by the
maintainer and turned into plans (010–013 above).

- **E. Ship the README's own "digest outside the Studio" recipe as real
  code**, not just prose. `README.md:325-342` walks through fetching
  per-editor state and applying `isDismissed`/`isSnoozed` by hand, then says
  outright that it "intentionally stays a recipe rather than shipped code."
  Every piece it describes is already exported or already exists internally
  (`splitItems`, `isDismissed`, `isSnoozed`) — wrapping them in one
  `buildDigest` export turns a doc example into something an integrator can
  actually import. → **Plan 010** (written, ready to execute).
- **F. A live badge on the Structure tool's own nav icon.** This plugin
  already tracks live signals (a snoozed item waking up, a dismissal expiring)
  that only become visible with the Inbox pane open. Whether Sanity's public
  component-override surface (`studio.components.navbar`, or a `Tool`'s own
  `icon`) can carry a live count was not confirmed during the audit — the
  type surface didn't grep cleanly. → **Plan 011** (spike; investigate first,
  build only if the mechanism checks out).
- **G. A team-wide "who's sitting on what" view.** `openTasks.ts` says
  outright, in its own comments, that "a fuller everyone's tasks view would
  need a project members list this source doesn't have yet." `unpublishedDrafts`
  already defaults to a shared, `'everyone'`-audience source, and already
  computes per-item assignees — but nothing in this plugin renders that
  outside one editor's own personal inbox. Whether a correct "still open,
  team-wide" count needs to fold in every editor's own dismissal state (found
  during recon to be queryable in one shot, since every editor's dismissals
  document shares one `_type`) is the open design question. → **Plan 012**
  (spike; effort L, framed as open questions for the executor).
- **H. Bulk "save a copy to my todos," from any source.** `MergedList.tsx`
  already has one bulk action that works regardless of which source a
  selected row came from (snooze — its own comment calls this out as
  "plugin-level, not per-source") alongside one deliberately gated to a
  single matching source (assign). A bulk "remember this personally," reusing
  the `todos` source's existing `create` capability against any selected
  row's title/description/due date, is a small, natural third one. → **Plan
  013** (ready to execute, smallest of the four).

## Lifecycle investigation, 2026-09-13 (second run)

Prompted directly by the maintainer: "when it keeps on living and life keeps
on going... things get finished, some stay unfinished... we need to take
care of that." Scoped to the direction/lifecycle theme against commit
`32e028c`. This codebase already has *some* lifecycle awareness — dismissals
TTL out after 90 days (`DISMISSAL_TTL_DAYS`), snoozes self-prune once their
wake time passes — so the investigation looked for where that awareness is
inconsistent or missing, not for a lifecycle story from scratch.

- **A todo marked done can silently reappear after 90 days.** `todos` has no
  `resolve`, so "mark done" only ever dismisses it (`InboxSection.tsx:170-177`),
  and dismissals age out (`dismissals.ts:25`) — a deliberate nudge for
  content sources (an ancient still-unpublished draft *should* resurface),
  but wrong for a todo, which has no other way to become "not done" again.
  `todos.ts`'s own `withoutTodo` doc comment already names this exact
  tension. **This is a correctness bug, not a direction option** → **Plan
  014**.
- **No cleanup path for departed editors' state.** `structureInbox.dismissals/
  snoozes/todos.<userId>` documents live in the customer's dataset forever;
  nothing ever deletes one, and there's no plugin-provided or documented way
  to find a departed teammate's leftover documents as team composition
  changes over a project's life. Distinct from the existing "Privacy note"
  finding below (that one is about *readability* of an *active* editor's
  state; this is *unbounded growth* from *departed* ones). → **Plan 015**
  (a pure diff helper + README recipe, same shape as `buildDigest` — not
  automated deletion).
- **Checked, not a finding**: a departed teammate's still-assigned draft/task
  already degrades gracefully — `assigneesById` is built from the *current*
  assignable-user list, so a stale `assignedTo` just falls back to the
  "Unassigned" placeholder rather than breaking. No action needed.
- **Direction D above ("Bounding the Done view by recency") — re-confirmed,
  downgraded further.** Its storage-level concern is already handled by the
  90-day dismissal TTL; what's left is only a UI nicety (hiding very old done
  items), not an unbounded-growth problem. The original "not obviously worth
  doing" verdict stands.
